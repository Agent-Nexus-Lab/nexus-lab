7月3日任务

当天目标：

把 6 月 21 日文档里最大的缺口补上：
  Cache 能真实命中；
  后端能返回 stage/progress/stage_message/cache_hit；
  timings 能覆盖完整阶段。
曹昕宇
任务：

1. 新建 backend/cache_backend.py：
   - 定义 CacheBackend 接口。
   - 实现 InMemoryCache 作为本地 fallback。
   - 实现 RedisCache，读取 REDIS_URL。
   - Redis 不可用时 fallback 到 InMemoryCache，并在 debug 中标记。

2. 新建 backend/plan_cache.py：
   - 实现 plan_result_cache key。
   - key 包含 profile_id、query_hash、date_scope、scoring_memory_hash、event_snapshot_hash、cache_version。
   - value 存完整 plan result。

3. 新建 backend/rewrite_cache.py：
   - 实现 rewrite_cache key。
   - key 包含 plan_items_hash、display_memory_hash、prompt_version、model_name。
   - value 存 summary 和 reasons。

4. 在 backend/plan_service.py 接入 cache：
   - 请求开始先查 plan_result_cache。
   - 命中则跳过完整 pipeline。
   - 未命中则正常执行。
   - LLM rewrite 前查 rewrite_cache。
   - cache 信息写入 debug.cache。
验收证据：

1. 第一次请求 debug.cache_hit=false。
2. 第二次相同请求 debug.cache_hit=true。
3. 第二次请求不调用 LLM rewrite。
4. debug.cache_type 能区分 none、plan_result、rewrite。
为什么今天安排这个：

Cache 是 6 月 16 日和 6 月 21 日反复要求但一直没做的核心性能能力。
7 月 2 日必须把骨架立起来，否则后面没有时间做真实验收。
马梓腾
任务：

1. 配合曹昕宇确认 ScoringMemory.cache_hash() 是否只包含排序相关字段。
2. 确认 DisplayMemory.cache_hash() 是否只包含文案相关字段。
3. 写两个测试：
   - recent_query_texts 变化不影响 ScoringMemory hash。
   - disliked_tags 变化会改变 ScoringMemory hash。
4. 准备一个 cache 验收数据集：
   - 同一 query 连续请求两次。
   - dislike 后再请求一次。
   - 验证 dislike 后 plan_result_cache 应失效。
验收证据：

1. hash 测试通过。
2. cache 验收数据集可以复用到 7 月 4 日端到端联调。
为什么今天安排这个：

Cache 的关键要求是防止错误复用。
马梓腾需要保证“哪些 memory 影响排序、哪些只影响文案”这件事在测试中被固定下来。
李颖哲
任务：

1. 在 experiments/agent_plan_runtime/llm.py 中增加 PROMPT_VERSION 常量。
2. 确认 rewrite_cache key 使用 PROMPT_VERSION。
3. 给 LLM rewrite 增加 structured fallback：
   - used_fallback
   - rewrite_error
   - timeout_seconds
   - model_name
4. 确认 ENABLE_LLM_REWRITE=false 时，plan-day 仍能返回模板 summary 和 reason_text。
验收证据：

1. LLM 正常时 debug.llm_rewrite.used_fallback=false。
2. 人为制造超时时 debug.llm_rewrite.used_fallback=true。
3. ENABLE_LLM_REWRITE=false 时接口仍返回 completed。
为什么今天安排这个：

rewrite_cache 的 key 必须包含 prompt_version。
如果 prompt 改了但缓存还复用旧文案，推荐理由会和新逻辑不一致。
同时 LLM 是最容易拖慢 loading 的环节，fallback 必须先补上。
张子恒
任务：

1. 根据后端最终字段，检查 loading.js 的字段读取：
   - runData.stage
   - runData.stage_message
   - runData.progress
   - runData.cache_hit
   - runData.debug.cache.cache_hit

2. 补 failed 状态结构化展示：
   - 优先 error_message。
   - 有 debug.rejections 时展示被过滤活动及原因。
   - 开发详情仍可展开或展示 raw JSON。

3. 准备 loading 录屏脚本：
   - 正常生成。
   - cache 命中。
   - failed case。
验收证据：

1. 前端能兼容后端字段缺失。
2. 前端能展示 cache 命中文案。
3. failed 状态要展示结构化错误信息，避免把整个 debug 生硬 JSON.stringify 给用户。
为什么今天安排这个：

后端今天会补 stage/cache 字段，前端必须同步对齐。
如果等到最后一天才看 UI，字段小差异会拖垮演示节奏。