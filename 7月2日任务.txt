7月2日任务
当天目标：

把“明明模块都写了，但中间没接上”的问题先修掉。
今天不追求大而全，先让 feedback -> memory -> search_events 的最短链路真实跑通。
曹昕宇
任务：

1. 在 experiments/agent_plan_runtime/runtime.py 中修改 _search_and_score()：
   - 增加 memory 参数。
   - 把 memory 传给 search_events(..., memory=memory)。

2. 在 plan_day() 中确认：
   - 调用 _search_and_score() 时传入 memory。
   - use_search_events 默认改为 True，或者 database/routers/agent.py 调用时显式传入 True。

3. 在 database/memory_service.py 或 database/routers/agent.py 中增加 dict -> Memory dataclass 的转换：
   - liked_tags 转 tuple
   - disliked_tags 转 tuple
   - negative_keywords 转 tuple
   - liked_event_ids 转 tuple
   - disliked_event_ids 转 tuple
   - recent_plan_event_ids 转 tuple
   - recent_query_texts 转 tuple

4. 在 PlanRun.debug 中写入 memory_used：
   - enabled
   - liked_tags
   - disliked_tags
   - negative_keywords
   - recent_plan_event_ids
   - memory_item_count
验收证据：

1. 本地单元测试或最小脚本能证明 search_events 收到了非空 Memory。
2. debug 中能看到 memory_used.enabled=true。
3. 至少一个活动的 score_components.memory.total_memory_delta 不等于 0。
为什么今天先做这个：

Memory 是第二阶段所有个性化能力的基础。
如果这条链路不通，前端反馈、memory_service、scoring 测试都只是分散成果，不能证明产品真的会“记住用户反馈”。
马梓腾
任务：

1. 基于现有 test_search_events.py，补一个端到端样例：
   - 构造一个用户 dislike 某类活动。
   - 调 search_events 前后对比排序。
   - 证明 disliked_penalty 或 repeat_penalty 进入 score_components.memory。

2. 对 existing memory scoring tests 做一次整理：
   - 空 memory 不改变分数。
   - liked_tags 加分。
   - disliked_tags 降分。
   - negative_keywords 降分。
   - recent_plan_event_ids repeat_penalty 降分。
   - liked/disliked 同时命中时不崩溃。

3. 输出一段可复制到验收文档里的 debug JSON 示例。
验收证据：

1. 测试命令输出通过。
2. 提供一份包含 total_memory_delta、details、explanation 的 debug 示例。
为什么今天安排这个：

曹昕宇负责把链路接上，马梓腾必须同时证明链路接上后排序真的变化。
否则容易出现“参数传了，但排序没有效果”的假闭环。
李颖哲
任务：

1. 检查 intent parser 中关于“不要”“不想看”“不感兴趣”“更喜欢”“轻松一点”的语义划分。
2. 确认哪些进入 hard_constraints，哪些进入 soft preferences，哪些应该写入 negative_keywords。
3. 补至少 5 条 eval case：
   - 不要创业路演
   - 不想看太商业的活动
   - 更喜欢 AI 展览
   - 今天下午不要太远
   - 不要重复推荐刚才那个
4. 写明每条 case 的期望解析结果。
验收证据：

1. eval.json 或等价测试文件新增 5 条 case。
2. 每条 case 都有 expected hard/soft/memory 语义。
为什么今天安排这个：

Memory 和 intent 的边界必须今天说清楚。
如果所有“不要”都被当成 hard reject，推荐会过度过滤；如果所有“不要”都只是软偏好，用户明确排除的条件又会失效。
张子恒
任务：

1. 对齐前端 feedback 请求体和后端 FeedbackEventRequest：
   - event_id
   - plan_id
   - plan_item_id
   - run_id
   - feedback_type
   - feedback_source
   - metadata

2. 检查 result.js 中 feedback_source 当前是 result_card，确认后端接受。
3. 修正文档中仍写 action 或 plan_result_card 的不一致表达。
4. 在前端失败 toast 中明确提示“反馈提交失败，请稍后再试”。
验收证据：

1. 点击“喜欢”能看到 POST /api/feedback/event payload。
2. 点击“不感兴趣”能看到 feedback_type=dislike。
3. 请求失败时前端能回滚按钮状态。
为什么今天安排这个：

Memory 闭环第一步来自前端反馈。
如果字段不一致，后端即使写好了也收不到正确行为，后面所有排序验证都会失去输入。
当天结束必须提交：

1. 每人至少一次提交或明确说明阻塞。
2. 群里同步：
   - 今日完成
   - 验收证据
   - 明日风险